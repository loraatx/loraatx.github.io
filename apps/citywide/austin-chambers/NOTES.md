# Deploy notes — Austin-Area Chambers of Commerce

Generated by the staging-intake skill from `assets/staging/`.

## Field picks

- `nameField`: `name` (every chamber has a unique name)
- Filters (1): `focus_type` (3 distinct categorical values among the 5 mapped chambers — Regional / Affinity / Geographic)
- Columns (4): `name`, `focus_type`, `street_address`, `city_state_zip`
- Popup (6): `focus_type`, `street_address`, `city_state_zip`, `website`, `map_location`, `notes`
- `markerColor`: `#6f4e37` (per `note.md`)
- `markerIconStyle`: `pin`
- `center`: `[-97.727, 30.379]` (mean centroid of the 5 mapped features)
- `sources` column: hidden, per `note.md` instruction

## Rows skipped (missing latitude/longitude in the staging CSV)

These 7 chambers are in the source markdown report and CSV but lack
geocoding, so they were not added to `data.geojson`. Fill in lat/lng in
`assets/staging/cd19bead (1).csv` and re-run the pipeline to add them:

1. Greater Austin Asian Chamber of Commerce
2. Greater Austin Hispanic Chamber of Commerce
3. Greater Austin Black Chamber of Commerce
4. Lake Travis Chamber of Commerce
5. Westlake Chamber of Commerce
6. Austin Young Chamber
7. Buda Area Chamber of Commerce

## Re-run

```
cd /path/to/loraatx.github.io
python scripts/staging_intake.py csv-to-geojson \
    --csv "assets/staging/cd19bead (1).csv" \
    --out apps/citywide/austin-chambers/data.geojson
```

…then commit and push.
